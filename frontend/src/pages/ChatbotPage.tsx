import ChatWidget from "../components/ChatWidget";

export default function ChatbotPage() {
  return (
    <section className="page">
      <h2 className="section-title">Election Assistant</h2>
      <ChatWidget />
    </section>
  );
}
